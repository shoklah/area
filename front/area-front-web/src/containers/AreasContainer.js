import { connect } from 'react-redux';
import AreasContainer from '../components/Areas';

const mapStateToProps = state => ({

});

const mapDispatchToProps = (dispatch, ownProps) => ({
});

const Areas = connect(
  mapStateToProps,
  mapDispatchToProps
)(AreasContainer);

export default Areas;